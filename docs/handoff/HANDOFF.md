# 引き継ぎ（2026-09-08）

> 最新: P1の掲載文・画像と基本申告は完了。掲載画像は所有者の指示で実在アニメ版に差し替え済み。末尾の追記を優先し、次回はP2から。

アニメログの改善・Google Play公開・収益化準備の最新状態。次回はP1から再開する。本番: https://animelog.jp/ 。

この文書が現在の再開点。ルートの RELEASE_HANDOFF.md / PLAY_STORE_SUBMISSION.md は2026年6月の旧資料。旧引き継ぎは [HANDOFF-2026-07-22.md](HANDOFF-2026-07-22.md) に保管した。旧資料にある個人アカウント前提、未配信、証明書待ち、人間によるDB適用待ち、Sonnet委譲体制は現在の状態として使わない。

## 完了したこと

- **Web公開済み**: PR #69〜73をCI成功後にsquashマージ。視聴中・話数記録、初回導線、保存/JSON・CSV書き出し/JSON復元、振り返り見本と関心ボタン、通報・ブロック、削除修正、プライバシー説明を反映。関心ボタンは購入ではなく、決済は未提供。
- **本番DB適用済み**: supabase/migrations/20260907000000〜20260907000400 の5件。進捗、記録取り込み、通報・ブロック、削除CASCADE、RLSのINSERT RETURNING補正。適用履歴と権限を確認済み。再適用しない。
- **Play内部テスト公開済み**: 組織アカウントRasideでアニメログを作成。package jp.animelog.twa。最新は **1.0.3 / versionCode 4 / targetSdk 36**。2026-09-08 10:56 JSTに公開し、Consoleの有効・最新リリース・内部テスターに公開を確認。
- 既存共有リスト **プライベートテスター（2名）** をアニメログで選択済み。ユーザー指定の2件目の追加も完了。新規リストや通知メールは不要。
- **Digital Asset Links本番反映済み**: アップロード鍵とPlay配信用アプリ署名鍵の2指紋を public/.well-known/assetlinks.json に保持。
- **Play掲載用アイコン**: public/icons/icon-512x512.png をConsoleの日本語デフォルト掲載情報に登録し、アイコン1/1で下書き保存を確認。ストア掲載全体はまだ一般公開されていない。
- **ホーム画面アイコン改善済み**: PR #74では図柄の切れを修正したが縮小しすぎた。PR #75で前景insetを16.666667%→12.5%へ変更し、図柄を12.5%拡大。1.0.3の追加実機画像にユーザーが「だいぶよくなったね」と確認。これ以上のサイズ調整は現在依頼されていない。
- **共通ナレッジ保存済み**: /Users/riku/.codex/skills/android-pwa-icon-release/SKILL.md と references/calibration.md。形式・参照・計算式・コピー内容を検証済み。他PJにも利用可能。12.5%を共通規格値とはしていない。

## Git・検証結果

- 保存時のブランチ main、HEAD **0a9a10bd5caba18f6bc0730f0774a306675fb7ce**。アプリ変更PR #69〜75はすべてマージ済み。
- #75の検証済み元コミット8a870b6とmainのツリー一致。mainのWeb CI/E2E **34178211453**、Android CI **34178211426**は成功。
- ローカル型チェック・単体224件・整形・Android署名ビルド/lint・bundletool validate・署名照合・完成APK内XML確認は成功。現在のlint警告バジェットは **131**（旧文書の136を使わない）。
- この引き継ぎで変更した文書はローカル保存のみ。コミット・push・PR作成はしていない。再開時は git status を確認する。
- 所有者の未追跡 .codex/、AGENTS.md、PLAY_STORE_SUBMISSION.md、RELEASE_HANDOFF.md を保持した。消さない。
- **docs/IMPROVEMENT_AND_MONETIZATION_PLAN.md は非公開のローカル戦略・作業記録。公開GitHubへの追加・push禁止。** 本文前半は段階当時の状態なので末尾の追記を優先する。料金案や事業方針はそのローカル文書で引き継ぎ、公開文書へ転載しない。

## 途中のこと

一般公開の審査提出は未実施。次は掲載文・スクリーンショット・フィーチャーグラフィック、Data Safety、IARC、対象年齢、審査用アクセス方法、解析設定を現行アプリと合わせる。実機でのログイン/保存/戻る/共有/オフライン復帰/削除の一式は未確認。アイコンのユーザー確認とアプリ全体の実機検証を区別する。

課金商品・Play Billing・サーバー購入検証は未実装。今回のアイコン共通化で別PJのコードやストアは変更していない。

## 直近の決定と理由

- ユーザーは組織アカウントを持つ。新規個人アカウントのテスター人数・日数条件をこのPJの前提にしない。
- ユーザーが「レビューなしで進めていいよ」と明示。今回の改善と公開作業についてClaude独立レビュー待ちを再発させない。実施済みの独立監査があるという意味ではなく、将来の無関係な高リスク作業への包括許可でもない。
- 本番SQLのCLI適用はユーザーが明示承認し完了済み。旧AGENTSの人間適用待ちを再開しない。
- AniListへの問い合わせはユーザーの希望で収益発生後へ保留。送信しない。利用許諾が確認済みになったわけではないので、課金開始時はローカル計画の未解決事項を確認する。
- ユーザーは繰り返しの確認を嫌う。すでに許可された修正・公開準備・内部配信は再承認を求めない。今回の引き継ぎ依頼だけを、新しい課金開始や未確定の法的申告への許可に拡張しない。

## 次にやること（P1→P4）

### P1: Play掲載情報の完成

1. この文書、[Play Data Safety案](../PLAY_DATA_SAFETY_DRAFT.md)、[プライバシー修正](../PRIVACY_RELEASE_REVIEW.md)、ローカル戦略記録の合意・末尾を読む。
2. Play Consoleで現在の掲載情報と未入力項目を再取得する。アイコンは下書き保存済み。内部テスト1.0.3の再アップロードは不要。
3. 現行機能に正確な日本語の短い説明・詳細説明を準備し、スクリーンショットとフィーチャーグラフィックを作る。app/、public/、scripts/take-screenshots.ts を参照。素材には合成データ等を使い、ユーザーの私的な記録を掲載しない。準備できる部分を完了してから、必要な不足情報だけ確認する。

### P2: 審査申告・実機確認

1. docs/PLAY_DATA_SAFETY_DRAFT.md と app/privacy/page.tsx、app/terms/page.tsx、app/delete-account/page.tsx を照合し、GA4・Vercel Analytics/Speed Insights・Annict・AniList・Googleフォーム・Supabase・Web Pushの実態を確認。
2. IARC、対象年齢、審査用アクセスと解析管理設定の未確定点を埋める。旧ドラフトの「外部送信先3つのみ」「すべての表現なし」を転記しない。
3. Play配信版で主要操作を確認。docs/RECORD_IMPORT_REVIEW.md、docs/MODERATION_REVIEW.md、docs/ACCOUNT_DELETION_REVIEW.md を使う。削除はテスト専用データに限定する。
4. 実装の修正が必要ならmainからcodex/ブランチ→関連チェック→PRで進める。

### P3: 一般公開の提出

P1/P2完了後に対象アプリ・トラック・申告・素材・バージョンを確認して既存のユーザー指示の範囲で進める。追加の法的同意等に明示回答が必要な場合だけ、その理由と具体的な対象を示す。内部公開済みを一般公開済みと扱わない。

### P4: 収益化の次段階

公開準備後、ローカル計画に従い利用・振り返り見本・関心イベントの計測を確認する。利用者の関心から候補を絞る。課金や外部問い合わせを勝手に開始しない。

## 再開に使う場所

- Console内部テスト: https://play.google.com/console/u/0/developers/4743734400463791816/app/4975615985531765720/tracks/4701462544686779607?tab=releases
- テスター参加リンク: https://play.google.com/apps/internaltest/4701462544686779607
- Consoleで一時アプリ名 jp.animelog.twa (unreviewed) が出る場合がある。掲載情報未審査によるもので別アプリではない。
- CUA最終既知: in-app browser ID 2 / tab ID 1。再開時にタブを確認し、古いAX番号をそのまま使わない。
- Android: android/README.md、android/app/build.gradle、android/app/src/main/res/drawable/ic_launcher_foreground.xml。
- 署名成果物: build/android-release/anime-log-1.0.3-api36.aab、同.apk、verification-1.0.3.json。AAB SHA256は 9efe846d3a34feb2e2a4de390cfbec3273d261c1b836dc9c0e81bd2294b25acf。
- 過去のビルド補助: /tmp/anime-log-sign-android.py、/tmp/anime-log-bundletool.jar。一時ファイルなので存在を先に確認する。
- JDK21: /opt/homebrew/opt/openjdk@21/libexec/openjdk.jdk/Contents/Home。SDK: /Users/riku/.bubblewrap/android_sdk。
- 既存鍵の保管元: ~/Downloads/アニメログ - Google Play package.zip。秘密値を表示せず署名時だけ安全に利用する。新しい鍵を作らない。

## 落とし穴・注意

- .env*は読まない・出力しない・コミットしない。署名鍵、パスワード、AAB/APKもGitへ入れない。公開リポジトリで git add . を使わない。
- 記録IDはUUID文字列またはローカル数値。DBアクセスはapp/lib/api/、通知はFeedbackContext。Next16のmiddleware相当は既存proxy.ts。dev/buildは--webpack。
- Play署名とアップロード署名は区別する。PWAアイコンだけ更新してもネイティブTWAアイコンは変わらない。
- 複数ランチャーで12.5%の安全性を確認したわけではない。再調整が必要になったら共通スキルを使い、元画像内の余白と実機の図柄比率から判断する。
- 旧SEO/性能バックログは退避文書に保持。現在の公開準備より先に古いP1を再開しない。状態や不具合の記述は再検証してから着手する。


## 2026-09-08 P1掲載情報完了・審査準備の追記（この節を優先）

次回は **P2の未確定項目** から再開する。P1の掲載文・画像作成を繰り返さない。一般公開の審査提出はまだしていない。

- 日本語の簡単な説明61文字・詳しい説明827文字を現行機能に合わせて作成し、Play Consoleへ保存。
- スマートフォン画像4枚（各1080×1920 JPEG）とフィーチャーグラフィック（1024×500 JPEG）を作成、目視・寸法・容量を確認してアップロード。掲載順は話数記録→クール別→ギャラリー→年別作品棚。既存アイコンを維持し、画像1/1・スクリーンショット4/8を確認。
- 掲載情報は「審査のために送信する準備ができました」で保存完了。今回作成した5画像にAIアセットのラベルを付け、再表示で保存を確認。既存アイコンの作成方法・AIラベル要否は今回確定していない。
- 合成した視聴記録6件・視聴予定6件と独自SVGの架空作品画像を使用。実ユーザー情報や既存アニメの画像は不使用。現行Webアプリのローカル画面撮影であり、Android実機の機能検証ではない。
- 保存先: `build/play-listing/2026-09-08/`。5画像、`overview.jpg`（全体プレビュー）、`listing-ja-JP.json`（説明文）、`assets.json`（SHA256）、`source/`（HTML・SVG・fixture）。既存gitignoreの/buildに含まれる。ローカル一時コピー `/tmp/anime-log-play-listing-20260908` は `.env*` を含まず、SupabaseはダミーURLで起動した。撮影用devサーバーは終了時に停止。
- 本番privacy/delete-accountをブラウザで確認。プライバシーURL、広告なし、広告IDなし、行政アプリではない、金融取引機能なし、健康関連機能なしをConsoleへ保存。1.0.3の生成済みrelease manifestにAD_ID権限がないことを確認。カテゴリは「アプリ / エンタテイメント」で保存。
- Consoleの「アプリのコンテンツ」の要注意は **4件**: ログインの詳細、コンテンツのレーティング、ターゲットユーザー、Data Safety。別途サポート連絡先が未入力（公開メール必須、Webサイトはanimelog.jpを予定）。
- 所有者へ質問済みで未回答: 13歳以上向けで進める想定、審査用専用アカウントの有無、Playで一般公開してよいサポートメール。パスワードをチャットへ求めていない。現行利用規約第3条は13歳以上。
- 本番GA4のSignals/広告連携・共有・保持設定等と実機の主要操作は未確認。IARCはAniListの画像・説明・公開UGC・外部リンクを含めて回答する。検索/詳細クエリにisAdult:false指定がないため「すべての表現なし」を根拠なく選ばない。
- 詳細と審査用アクセス手順案: [Play掲載情報・審査準備](../PLAY_LISTING_PREPARATION.md)。残る設定の入力後に一般公開の提出へ進む。Claudeレビュー待ち、完了済みDB、アイコン再配信、AniList問い合わせを再開しない。
- 既存の未コミット・未追跡ファイルを保持。新しい資料もローカル保存のみで、コミット・push・PR作成なし。非公開戦略文書をGitへ追加していない。
- Console再開タブ: in-app browser 2 / tab 1、`.../app-content/overview`。新しいAX状態から操作する。


## 実在アニメ版の掲載画像へ差し替え済み（2026-09-08 最新）

所有者が「では載せましょう」と指示したため、6作品の実際の画像・作品名を使って4枚を再撮影し、Play Consoleへ差し替えて保存完了。対象はフリーレン、ぼっち・ざ・ろっく、ゆるキャンSEASON3、SPY×FAMILY Season2、薬屋のひとりごと、ダンジョン飯。視聴記録・評価は合成データで、その旨を画像内に表示。画像の出典と放送クールはアプリのAniList検索結果から確認した。

現行素材は `build/play-listing/2026-09-08/real-anime/` の `01-watching-real.jpg`〜`04-recap-real.jpg`、全体プレビューは `overview-real.jpg`。各1080×1920。旧4枚は掲載枠から外したがローカル・アセットライブラリに保持。掲載順は以前と同じ。AI作成の見出し・レイアウトを含むため新4枚にもAIアセットラベルを設定。既存フィーチャーグラフィック・アイコン・掲載文は変更なし。

Consoleで新4ファイルの順序と「変更を保存しました。[公開の概要] で審査に送信してください。」を確認。一般公開は未申請。詳細は [掲載準備資料](../PLAY_LISTING_PREPARATION.md) 末尾。先の「架空作品のみ」「実在画像不使用」は旧版の状態として扱う。利用許諾を別途取得したという意味ではなく、AniList問い合わせは引き続き保留。P2の残件・確認事項は前節のまま。コード・DB・内部テスト配信の変更はしていない。


## 2026-09-08 サポート連絡先の候補入力（未保存）

所有者がサポートメール候補として `info@raside.com` を提示し、組織アカウントでも必要か質問。Google公式ヘルプ（https://support.google.com/googleplay/android-developer/answer/113477?hl=ja）と現行Consoleで、アプリ単位の公開サポートメールが必須であることを確認。ログイン用アカウントとは別欄。

Consoleのストア設定にメール `info@raside.com`、Webサイト `https://animelog.jp/` を入力。最初の「保存して公開」で確認ダイアログを開いたが、最後の確定操作は自動承認レビューが拒否した。理由: 「かな」という候補提示は、このメールとURLをすぐに一般公開する確定承認ではない。最終保存・公開は未実施。入力値はフォームに保持されているが、一覧へ反映したように見える楽観的表示を保存完了の証拠にしない。次はこのメールとURLの公開への明示回答を受けて最終確定する。Googleアカウントや組織情報を変更する操作ではない。一般公開リリース申請もしていない。


## 2026-09-08 公開サポート連絡先の登録完了（最新）

所有者が、メール `info@raside.com` とWebサイト `https://animelog.jp/` をPlayの公開連絡先として保存することを「お願いします」と明示承認。既存の確認ダイアログから「保存して公開」を実行した。Consoleがスキーム省略URLをHTTPに補完したため、承認済みの完全なHTTPS URLを入力し直して保存・公開。最終的にメール `info@raside.com`、URL `https://animelog.jp/` と「変更内容が公開されました」を確認した。任意の電話番号は未設定。前節の自動承認レビューによる停止条件は解消済みで、再承認・再入力は不要。

変更対象はアニメログのアプリ別サポート連絡先のみ。Googleアカウント・組織プロフィールは変更していない。一般公開リリースの審査提出は未実施。残件は審査用ログイン、IARC、対象年齢、Data Safety/解析設定、実機主要フロー確認。既存の未コミット変更を保持し、コミット・pushはしていない。

## 2026-09-08 P2調査・Data Safety未公開版保存（最新）

- 所有者が「調べた上で進めてください。どうしてもわからないところは私がやります」と指示。本番GA4を既存ログインから調査し、コード・Google公式定義と照合してData Safety全10種類を入力。「未公開として保存」→保存完了を確認。最終保存は対象年齢の未完了により無効。詳細は [最新Data Safety記録](../PLAY_DATA_SAFETY_DRAFT.md)。入力し直さない。
- Consoleの依存順は **審査用ログイン情報→対象年齢→Data Safety最終保存**。利用規約は13歳以上なので対象年齢は13〜15、16〜17、18歳以上を準備。対象年齢を確認するだけの質問は再発させない。
- GA4: 本番ストリーム受信あり、GoogleシグナルOFF、ユーザー提供データOFF、Google広告リンク0件。イベント保持2か月、ユーザーデータ14か月、活動時リセットON。地域・端末の詳細収集ON。Googleプロダクト/サービスへの共有OFF、集計・匿名化モデリングON。解析設定は変更していない。
- 審査用アカウントは資料に実在・利用可能と確認できるものがない。CIの認証は秘密値参照のみで、`.env*`や秘密値は読んでいない。所有者へ受信可能な専用メールで登録・メール確認・ログインを依頼し、新規登録画面を用意。例示した`info+playreview@raside.com`は未確認の候補であり、アカウント作成済みではない。パスワードはチャットやGitに記録せず、所有者がConsoleへ直接入力する。
- IARC開始前に規約同意が伴う表示を確認。所有者へ [IARC規約](https://web.iarcservices.com/terms) の同意を操作時に質問済み。回答待ちの間は質問票へ進んでいない。Claudeレビュー待ちではなく、ブラウザー操作ツールの契約同意ルールによる確認。
- 英語の審査用説明449文字と実機チェックリストを [掲載準備資料](../PLAY_LISTING_PREPARATION.md) 末尾に保存。アカウント削除テストは審査アカウントとは別の専用アカウントで行う。実機の主要フローは未完了。
- 一般公開未申請。コード、DB、Android配信、掲載素材は変更なし。既存の未コミット差分・非公開戦略文書を保持し、コミット・pushなし。
- 最後にConsoleのログイン情報追加ダイアログへ名前`Google Play review account`と英語手順449文字を入力して保持。メール・パスワードは空、全機能アクセス確認は未選択、追加・保存は未実行。既知タブはin-app browser 1 / tab 7（Console）とtab 10（本番の新規登録画面）。再開時に一覧・状態を取り直す。新規登録画面は本人操作用に開いてあり、資格情報は未入力。

### 公開地域の指定（2026-09-08）

所有者が「だすのにほんだけでいい」と指定。一般公開の配信対象は日本のみとし、製品版の国/地域設定を提出前に確認・設定する。この時点では地域設定の保存はまだ行っていない。審査担当者向けの英語アクセス手順はConsoleが要求しているもので、英語圏への配信を意味しない。画面を再確認したところ、本番タブ10は未ログインのホーム画面に戻っており、新規登録ダイアログは閉じている。

### 公開地域の追加指示（上の日本限定を更新）

所有者が「ヨーロッパ以外も問題なく出せるならいい。ヨーロッパは面倒」と追加指定。欧州を配信対象から外し、欧州以外は現行の無料・非ゲームアプリに適用される追加条件を確認した地域へ広げる方針。欧州には少なくともEEA・英国・スイスを含め、EU加盟国だけを外して済ませない。非欧州全地域への無条件の適合を確認したわけではなく、製品版の地域設定はまだ未保存。

Google公式の国別要件では、ブラジルの記載は売上を受け取る販売者向け、韓国GRACの追加認定はゲーム向けなど条件が分かれている。現在の無料・非ゲームという実態に照らして確認する。根拠: https://support.google.com/googleplay/android-developer/answer/6223646?hl=en-GB 。Playの地域除外はWeb版へのアクセス遮断にはならず、欧州等からのWebアクセスについてGA4の同意要件まで不要になるとは扱わない。根拠: https://www.google.com/about/company/user-consent-policy/ 。IARC同意・審査用アカウントの本人操作待ちはこの地域指定によって解消していない。

## 2026-09-08 製品版リリース保存・4地域指定（最新）

- 所有者が欧州除外の方針での続行を承認し、アニメログにログインしたと連絡。アプリのログイン成功を確認したが、既存の視聴記録があるアカウントだった。審査専用としてGoogleへ渡す許可は未確認のため、別の専用アカウントを用意するか、今のアカウントが審査専用かを質問中。個人の記録は編集・削除せず、認証情報を取得・転記していない。ユーザー名・作品数等を公開文書へ転載しない。
- 本番の配信対象を **日本・台湾・香港・韓国の4地域** として保存。「公開の概要」に4地域の追加が審査未送信の変更として反映されたことを確認。欧州（英国・スイス含む）やその他の未確認地域を一括選択していない。
- 国別要件の追加調査で、ブラジルのDigital ECAによる年齢情報取得の条件、米国の一部州の年齢確認・保護者承認等の条件を確認した。前節のブラジル説明は販売者確認しか見ておらず不十分だったため訂正。これらは初回対象に含めていない。公式根拠・判断の範囲は [リリース状況](../PLAY_RELEASE_STATUS.md) 末尾。
- 製品版トラック **4698456132766618693** に **release 1 / `1.0.3 (4) 初回公開`** を作成。既存ライブラリのversionCode4 / 1.0.3 / targetSDK36を選択し、日本語リリースノートを保存。「リリースの準備が完了しました」を確認し、プレビューから保存→「変更を保存しました。審査のためにGoogleに送信するには公開の概要へ」の通知を確認。審査未送信。既存内部テストの再配信・バイナリ再アップロード・再ビルドはしていない。
- Consoleのアクセス情報フォームには名前・英語449文字が未保存入力として残る。メール・パスワードは空。アプリへのログインだけでは審査用ログイン情報は完成しない。対象年齢→Data Safety最終保存の依存も未解消。IARC規約同意はまだ明示回答を確認できていない。
- 既知のin-appタブ: 7=ログイン情報追加フォーム、10=ログイン済み本番アプリ、11=公開の概要、12=IARC開始画面。操作前に現行状態を取り直す。一般公開未申請、実機主要フロー未完了。

## 2026-09-09 IARC同意・審査アカウント承認、API停止（最新）

- 所有者が、今ログイン中のアカウントをGoogle審査に提供することと、IARC利用規約への同意の**双方を明示承認**した。既存記録を審査担当者が閲覧・編集・削除できることも確認文で説明済み。専用アカウントへの変更や規約同意を再度求めない。承認はこのアカウントの審査提供であり、エージェントが既存記録を試験目的で削除する包括許可ではない。
- IARCは`info@raside.com`、カテゴリ「その他のすべてのアプリの種類」で規約同意を選択して質問票を開始した。質問票の途中保存で「変更を保存しました」を確認。レーティングの取得・申告送信はまだ完了していない。
- ユーザーコンテンツ共有「はい」の操作は一度自動承認レビューに拒否された（音声/SMS/画像共有とプロフィール・感想を混同しているとの理由）。Google公式ヘルプ11070862の英語・日本語でコメント投稿と写真共有も対象と確認し、プロフィール画像アップロード・公開URL、感想表示のコードを照合した後の再操作は成功。拒否は解消済み。これを未解決の承認待ちとして再発させない。
- 保存済みIARC回答: 同梱の要レーティングコンテンツなし、ユーザーコンテンツ共有あり、共有UGCが主たるコンテンツ源ではない、ヌードや実際の露骨な暴力表現の投稿を許可しない、ブロックあり、通報あり、チャットモデレートなし、招待友人のみへの制限なし、オンラインカタログあり。暴力テーマあり・主目的ではない・視覚/テキスト表現ありを入力したが、画像の残酷さ等の詳細は未回答。後半の回答は、API復旧後にアプリが実際に表示する範囲を再照合してから確定する。配信作品本編の表現を、そのまま本アプリ内の表現と混同しない。
- 本番で作品検索を実行したところ、ブラウザーに`[queryAniList] Failed to fetch`、画面は0件を表示。公開AniList APIへ認証・個人情報なしの読み取りクエリで確認した結果、**403 / `The AniList API has been temporarily disabled due to severe stability issues.`** を返した。提供元の一時停止が原因。検索0件を作品不在と判定しない。AniListへの問い合わせは引き続き送信していない。検索確認とカタログ表現の詳細確認は復旧後に行う。
- Google審査用の認証欄は、値を出力せず入力有無だけ確認した結果、メール・パスワードとも空。所有者へConsoleへの直接入力を依頼済み。認証情報の提供承認はあるが、パスワード自体は不明でログインセッションから復元できない。`.env*`や保存パスワードを探索していない。入力されたら資格情報をログ出力せず、追加・保存→対象年齢→Data Safety確定を進める。
- タブ: in-app browser2、7=認証欄（名前・英語手順入力済み）、10=ログイン済み本番ホーム（検索モーダルは閉じた）、11=公開の概要、12=IARC途中保存。製品版1.0.3と4地域は前節どおり保存済み・審査未送信。コード・DB・個人記録・Android配信の変更なし。

## 2026-09-09 検索障害表示を本番へ反映（最新）

- 続行指示を受け再確認したが、AniList公開APIの停止（403、安定性問題のメッセージ）は継続。Consoleのメール・パスワード欄も空のまま。入力有無だけ確認し、資格情報を表示・探索していない。
- 停止を0件として表示・キャッシュしてしまう不具合を修正し、[PR #76](https://github.com/Jilvert22/anime-log/pull/76)をマージ。本番Vercelの成功を確認。mainは **dd67c5f02884eecbcdad214b8c9fc3bfbaaa00a4**。元の作業ツリーもfast-forwardで更新し、未コミットの文書を保持。
- 作品追加・積みアニメ・来期視聴予定の検索に障害案内と再試行を追加。クール欄からの検索はFeedbackContextで失敗を通知。明示的な検索ではAPIの失敗を伝え、失敗結果はキャッシュしない。正常な0件や、Annictだけの補助情報取得失敗、既存の補助検索APIの挙動は維持。
- 手動追加の案内を訂正: 現行の追加画面には手動入力の導線がなかった。以前の「手動追加で記録を続けられる」という説明・審査手順案は不正確。今回の修正は登録済み記録の利用を案内するもので、手動追加機能の新設ではない。
- 検証: ローカル型チェック、Vitest232件、Lintエラー0・警告131（増加なし）、Prettier、本番ビルド成功。PR CI/E2E **34249037604** とVercel Previewも成功。修正元コミット **7cac7c62a86813284f6e7effd97de51aab88927a**。本番Vercel deployment **7ieuAdNotPatMehkuwXkkqfnaKry** はsuccess。
- 修正用worktree `/tmp/anime-log-search-outage`、ブランチ`codex/anilist-search-outage`を保持。`.env*`なし、node_modulesは既存インストールへのリンク。ソースとテスト10ファイルだけコミット・push。引き継ぎ文書と非公開戦略文書はPRに含めていない。
- API提供元の停止は未解消。IARC詳細・検索の正常系・実機主要フローは未完了。審査用認証情報の直接入力→対象年齢→Data Safety確定も残る。Play審査未送信。DB・Androidバイナリ・配信地域・ユーザーの記録は変更していない。


## 2026-09-09 通常検索の取得確認とカタログ制限の未決事項（最新）

- PR #76の本番反映後、実際の追加画面で「チェンソーマン」3件、「峰不二子」2件の通常検索結果を取得した。別の最小公開APIクエリは同時期にも停止メッセージ付き403だったため、全面復旧とは断定しない。登録・既存記録の編集や削除はしていない。障害UIは自動テストで検証済みだが、今回の本番再確認では正常結果が返り、本番で障害UIが表示されたとは記録しない。
- 実際の検索結果が参照する表紙を確認。チェンソーマンの血の描写に基づき、IARCの視覚的暴力の残酷/不快な画像ありを入力。峰不二子のカタログ表紙も確認し、性的テーマあり・それが主目的ではない・視覚表現ありまで回答して途中保存（「変更を保存しました」確認）。ヌード・ポルノの可能性などは未回答。これらは最終確定前の回答で、アニメ本編を本アプリが配信しているという申告ではない。
- 公開前の重要な未決事項: 現行のAniList検索/詳細クエリには`isAdult: false`等の制限がない。AniList公式は成人向け内容が含まれ得ること、Ecchiは成人向け判定に含まれないこと、追加の確認・表示制限が必要なことを説明している。Google Playはカタログでもポルノを許容せず、性的内容から未成年を保護する制限を要求している。この状態で「性的画像なし/ポルノの可能性なし」と断定してIARCを確定しない。
- 所有者へ、公開版の検索から「成人向け」と「Ecchi」を除外する方針を非同期質問した。一部の一般放送作品も対象外となるため、商品仕様として選択してもらうもの。既存記録は削除しない。回答前にフィルター変更は実装・公開していない。元の画像掲載への承認やIARC規約承認を再度求めるものではない。
- Google審査用のメール・パスワード欄は入力有無のみを再確認し、両方空だった。本人の直接入力が必要。IARC同意・今のアカウントを審査に使用する許可は解消済みのまま。一般公開審査は未送信、実機主要フロー未完了。
- 公式根拠: [AniListの注意事項](https://github.com/anilist/docs/blob/master/docs/guide/considerations.md)、[AniListのクエリ引数](https://docs.anilist.co/reference/query)、[Google Playの不適切なコンテンツ](https://support.google.com/googleplay/android-developer/answer/9878810?hl=en)。`isAdult: false`だけでGoogleの全要件に適合する保証はない。

終了時: 確認用画像タブ14・15を閉じ、本番の検索モーダルはEscapeで閉じた。最後のブラウザー状態確認でMacがロックされ自動解除できないとの通知。これ以降の画面操作はしていない。再開時は本人のロック解除後に現行タブ状態を取り直す。成人向け/Ecchi除外の質問は回答未受領。


## 2026-09-09 成人向け検索除外の本番反映・IARC最終保存（最新）

- 所有者が「はい、それらもお願いします」と回答し、成人向け・Ecchiの除外と残作業の続行を承認。前節の検索範囲の方針確認待ちは解消。再確認不要。
- [PR #77](https://github.com/Jilvert22/anime-log/pull/77)をマージし、本番反映を確認。main **a66ae6009858540b172c36c73171f2a37fba3f08**、元ソースコミット`c538600`。タイトル/クール検索・ID指定詳細取得に`isAdult: false`と`genre_not_in: ["Ecchi", "Hentai"]`を追加。返却結果でも成人向け・該当ジャンル・分類欠落を除外する。既存記録の変更・削除はない。放送状態だけの一括取得は維持。
- これは新規のカタログ検索・詳細取得の制限。過去に保存された画像、インポートされた記録、ユーザー投稿を一括監査・削除する変更ではない。AniListの分類の完全性や、ストアポリシー全般への適合を保証するものではない。
- 検証: 型チェック成功、Vitest **240件/34ファイル**成功、Lintエラー0・警告131、Prettier/diff check成功。PR CI/E2E **34299285377** 成功（3分18秒）、Vercel Preview **FUUSdnF6wSn1Kpm7jSbTDr8G8NVN** 成功。本番deployment **3JGKwyWLohmTQCJaWBG7uNY2epDe** のsuccessを確認。ローカルmainをfast-forward更新し、未コミット資料を保持。
- 本番を再読込して、一般作品「チェンソーマン」3件、2026年春のクール検索51件、除外確認用「異種族レビュアーズ」0件を確認。通信エラー表示ではなく通常の0件表示だった。個人の作品登録/編集/削除は行っていない。通常検索は利用できたが、提供元全体の安定性が全面回復したという判断ではない。
- **IARC全設問の回答と最終保存を完了**。「変更を保存しました。[公開の概要]で審査に送信してください」を確認し、公開の概要へ「コンテンツのレーティング / 新しいアンケートを送信」が追加された。Googleの一般公開審査への送信は未実施。
- IARC算出結果: 日本等のIARC Generic **12歳以上**、韓国 **12歳以上**（どちらも保護者の指導推奨/ユーザーインタラクション）。北米13歳以上、ブラジル16歳以上、ドイツ16歳以上、PEGI保護者判断推奨。後者の地域を新たに配信対象へ追加したわけではない。初回4地域と製品版1.0.3 (4)を維持。
- IARC後半: カタログには暴力（流血画像を含む）・性的テーマ/視覚表現/テキスト・不快/卑わいな言葉・薬物に関するテキストがあり、それらへのアクセスがアプリの主目的ではない。公開予定のカタログ制限と確認できた表紙に基づき、性的ヌード/ポルノ画像なし、差別的言葉なし、違法薬物の視覚描写なし。年齢制限商品の販売宣伝、詳細位置共有、デジタル購入、現金/NFT等報酬、汎用ブラウザ/検索エンジン、主目的がニュース/教育はいずれもなし。単に全設問を「なし」としていない。作品本編を配信しているという申告ではない。
- 説明文の確認資料としてAniListの[Detroit Metal City](https://anilist.co/anime/3702/Detroit-Metal-City)の説明・ジャンル・タグを参照。AniList説明は作品詳細画面のAnnictあらすじがない場合の表示に使用される。確認済み表紙の根拠は前節。
- **現在の阻害要因は審査用メール・パスワードの未入力**。新しい入力画面に名前と英語449文字を再入力して保持したが、2欄は空のまま（値を表示せず有無だけ確認）。所有者へConsoleの2欄へ直接入力して「入力済み」と知らせるよう非同期依頼。現在のアカウントを審査へ提供する承認は取得済みであり、新規アカウント作成や再承認は不要。
- 対象年齢の画面で「ログインの詳細セクションに情報を入力してください」というブロックを再確認。依存順は引き続き **ログイン情報→対象年齢→Data Safety最終保存**。公開の概要のクイックチェックの2問題も「ターゲットユーザー未完了」「Data Safety未完了」。Data Safety10種類の未公開版は作り直していない。
- 実機について、本人へ内部テスト1.0.3でログイン→作品追加/話数→再起動後の保存、戻る、オフライン復帰の確認を非同期依頼。未回答なので完了扱いにしない。審査アカウントや既存記録の削除は依頼していない。ブラウザーでの正常検索やCI/E2Eは実機確認と区別する。
- Consoleの9月30日期限のAndroidデベロッパー確認通知を調べたところ、ホームには「すべてのアプリの登録が完了し、Androidデベロッパーの確認要件を満たしています」と表示。追加登録・組織情報変更は行っていない。
- 現行タブ（in-app browser2）: **16=審査ログイン情報の入力ダイアログ、17=対象年齢（ログイン情報待ちの案内）、18=ログイン済み本番ホーム**。3タブにmarkHandoffを設定。検索モーダルは閉じた。タブ19の確認用Consoleホームは閉じた。
- 作業ツリー`/tmp/anime-log-catalog-filter`、ブランチ`codex/catalog-content-filter`を保持。ソースとテスト2ファイルのみPRへ追加。`.env*`を読まず、非公開戦略文書・未コミット引き継ぎ文書をGitHubへ追加していない。DB・アイコン・Android内部配信・AniListへの問い合わせは行っていない。


## 2026-09-09 審査ログイン・対象年齢・Data Safety完了、最終送信の明示承認待ち（最新）

- 所有者がConsoleへの認証情報入力を「にゅうりょくずみ」と連絡。メール/パスワードは入力有無のbooleanのみ確認し、値を表示・保存・転記せず、既に承認済みの審査アカウントとして全機能アクセスのチェック→追加→保存を実行。「変更を保存しました。[公開の概要]で審査に送信してください」を確認。**審査用ログイン情報の入力待ちは解消。再入力を依頼しない。**
- 対象年齢は利用規約どおり13〜15歳、16〜17歳、18歳以上を選択し、最終保存完了。IARC ESRB13歳以上のため13歳未満は選択不可。対象年齢のログイン情報待ちブロックは解消。
- Data Safetyは保存済み10種類の回答を維持してステップ5のプレビューを確認し、最終保存完了。個人情報3、位置1、写真1、診断1、アクティビティ3、端末ID1。検索語の共有、削除URL、転送時暗号化、プライバシーURLを確認。新たな種類の申告やGA設定変更は行っていない。
- 公開の概要に**10件の未送信変更**が表示され、申告不足の問題表示はなく「10件の変更を審査に送信」が有効になった。製品版`1.0.3 (4) 初回公開`、日本/台湾/香港/韓国の4地域、掲載情報、IARC、対象年齢、プライバシー、広告、Data Safety、健康、カテゴリが対象。管理対象の公開はオフ。ログイン/広告ID/行政/金融の申告も審査用情報に記録された。
- 最初の「10件の変更を審査に送信」で最終確認ダイアログを開いた。最終の**「変更を審査に送信」は自動承認レビューが拒否**。理由:「Google Playの製品版・地域設定・掲載情報を含む10件を審査へ送信する不可逆性の高い公開手続きで、ユーザーによるこの最終送信の明示承認が確認できません」。送信は未実施であり、審査中/公開済みとは扱わない。
- 所有者へ、上記10件をGoogle審査に送信する最終操作の明示承認を依頼する。これはClaudeレビューの要求ではなく、ツールの自動承認レビューによる停止。今のアカウント提供、IARC規約、成人向け除外への承認は取得済みのまま。明示承認後は保存済み項目を繰り返さず、最終送信→送信状態の確認へ進む。
- Googleのクイックチェックはこの時点で処理中（残り約14分の表示）。送信予約の後、チェック完了時に送信される説明があるため、最終確定後の画面を確認して「送信予約/審査中」を区別する。確認ダイアログの審査期間案内は通常7日以内、より長い場合あり。
- 実機の追加確認（保存/再起動/戻る/通信復帰）は本人の結果報告がなく未完了のまま。CI/E2Eやブラウザー検索成功と混同しない。
- in-app browser2: tab20=保存済みログイン情報、tab21=公開の概要、**tab22=公開の概要の最終送信確認ダイアログ**。tab22をユーザー向けに開く操作と保持設定を実施。資格情報は画面キャプチャや文書へ記録していない。コード・DB・Android配信の変更なし。

終了時訂正: 拒否後にタブ22の保持を試みたところタブが消えており、一覧にはユーザーのtab20（ログインの詳細）のみ残っていた。保存済み公開概要をtab20へ表示しようとした読み取り移動も、自動承認レビューが「拒否後に同じ公開操作を再試行するための回避手段になり得る」として拒否。これ以降Consoleへ再アクセスしていない。tab22の確認ダイアログは保持できていない。本人が最終審査送信を明示承認した後に、利用可能タブを確認して公開の概要へ進むこと。


## 2026-09-09 10:52 JST 一般公開の審査送信完了（最優先の最新状態）

- 所有者が、Android1.0.3を日本/台湾/香港/韓国で公開するための10件の変更をGoogle審査へ送信する最終操作に「はい、お願いします」と明示承認。前節の自動承認レビューによる停止条件は解消。再承認を求めない。
- tab20の公開の概要で事前チェック完了（「変更内容を審査に送信できるようになりました」）を確認し、「10件の変更を審査に送信」→最終確認の「変更を審査に送信」を実行。
- **Consoleで「審査中の変更」「変更内容は現在審査中です」を確認。一般公開の審査送信は完了。** 送信予約や未送信下書きのままではない。一方で、Googleの承認・一般公開完了はまだ確認していない。
- 審査対象: 製品版`1.0.3 (4) 初回公開`の完全公開、日本/台湾/香港/韓国の4地域、JP掲載情報、IARC、対象年齢13歳以上、プライバシーURL、広告申告、Data Safety、健康申告、カテゴリの10件。ログイン情報/広告ID/行政/金融の審査用申告も同じ画面に反映。
- 管理対象の公開はオフのまま。公開地域やリリースを増やす変更は行っていない。確認ダイアログでは審査は通常7日以内、長くなる場合ありと案内された。
- 今後は審査結果（承認/差し戻し）を確認する。審査中の変更を削除したり、同じ申告を再送信したりしない。能動的な通知・定期監視は依頼されておらず、スケジュールは作成していない。
- 実機の追加確認（保存/再起動/戻る/通信復帰）は結果報告未受領のため、完了扱いにはしていない。既存の内部テストやアイコン配信はやり直していない。
- ユーザーのin-app browser2 / tab20を「公開の概要」（審査中）として保持。認証情報は読出し/ログ出力/文書記載していない。ローカル資料への追記のみで、Gitコミット/push、DB、Webコード、Androidバイナリの変更はない。
